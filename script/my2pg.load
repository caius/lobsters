LOAD DATABASE
FROM mysql://root:localdev@localhost/lobsters_dev
INTO pgsql://caius@localhost/lobsters_dev

WITH include drop, create tables, create indexes, preserve index names, foreign keys,
  reset sequences, downcase identifiers

CAST type bigint when unsigned to bigint
